

fun a(uint32 a, uint32 a): void // expect compile error (redecl-param)
    boom

fun b(uint64 b, uint32 b): void // expect compile error (redecl-param)
    var x: uint32
    b = x // expect compile error (assign-conflict-tys)
